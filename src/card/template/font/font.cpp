#include "font.hpp"

namespace Card
{
  namespace Template
  {
    namespace Font
    {
      Base::Base(std::string name, Type type, Size size) : name(name), type(type), size(size)
      {
      }
    }
  }
}